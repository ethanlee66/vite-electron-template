{
  "name": "vite-electron-template",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "main": "dist/main/electron-main.cjs",
  "scripts": {
    "dev": "vite --config packages/renderer/vite.config.ts",
    "electron:start": "cross-env MODE=development node scripts/start-electron.js",
    "electron:build": "cross-env MODE=production node scripts/build-electron.js && electron-builder --config build/electron/builder.yaml",
    "build": "vue-tsc --noEmit --p packages/renderer/tsconfig.json && vite build --config packages/renderer/vite.config.ts",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "preview": "vite preview"
  },
  "dependencies": {
    "vue": "^3.4.29"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.0.5",
    "cross-env": "^7.0.3",
    "electron": "^31.2.0",
    "electron-builder": "^24.13.3",
    "typescript": "^5.2.2",
    "vite": "^5.3.1",
    "vite-plugin-node-polyfills": "^0.22.0",
    "vue-tsc": "^2.0.21",
    "wait-on": "^7.2.0"
  }
}
